export type DayDirection = {
  startingPoint: string;
  change: string;
  endState: string;
  plan: string[];
  unlocks: string;
};

export const projectStages = [
  { week: 1, short: "Working product", detail: "Make one question become a checked report." },
  { week: 2, short: "Saved progress", detail: "Keep runs safe through restarts." },
  { week: 3, short: "Shared work", detail: "Let specialized workers cooperate." },
  { week: 4, short: "Proof and delivery", detail: "Run, test, measure, and present it." },
];

export const directionByDayId: Record<string, DayDirection> = {
  "day-01": {
    startingPoint: "You have an empty project folder and no EvidenceLab workflow yet.",
    change: "Set up Python safely, make one asynchronous model call, and observe how waiting tasks can overlap.",
    endState: "A small program prints a model answer and token usage, and you can explain why async work matters for later web research.",
    plan: ["Create and activate a Python environment.", "Keep the API key outside Git.", "Send one request through the OpenAI SDK.", "Compare sequential and concurrent fake requests."],
    unlocks: "Tomorrow you can ask the model for a predictable research plan instead of unstructured text.",
  },
  "day-02": {
    startingPoint: "The app can call a model, but the answer is free-form text that your code cannot safely coordinate.",
    change: "Define a typed research-plan shape and require the model response to match it.",
    endState: "One broad question reliably becomes 3–5 small ResearchTask objects with clear limits.",
    plan: ["Define the request and plan models with Pydantic.", "Ask for a structured model response.", "Validate task count and research limits in code.", "Deliberately test one invalid plan."],
    unlocks: "Tomorrow each typed task can be sent to its own web researcher.",
  },
  "day-03": {
    startingPoint: "The planner creates focused tasks, but those tasks do not collect evidence yet.",
    change: "Research each task on the web, save source details, and limit how many searches run together.",
    endState: "The app produces a bounded evidence bundle for every task and reports its source and token usage.",
    plan: ["Define the source and evidence data shapes.", "Research one task with web search.", "Run independent tasks concurrently behind a safety limit.", "Inspect partial failure and usage totals."],
    unlocks: "Tomorrow the report writer can combine real, stored evidence rather than inventing facts or links.",
  },
  "day-04": {
    startingPoint: "The app has evidence and source URLs, but a model could still invent or alter a citation link while writing.",
    change: "Give every saved source an internal ID, let the model cite only those IDs, and let code insert the real URLs.",
    endState: "A structured report is rejected if it cites an unknown source and is rendered with only verified links.",
    plan: ["Build a numbered source catalog.", "Define the structured report format.", "Ask the model to cite source IDs only.", "Validate every ID, then render Markdown links in code."],
    unlocks: "Tomorrow you can place the trusted question-to-report flow behind a web API.",
  },
  "day-05": {
    startingPoint: "The complete workflow runs from Python, but other programs have no stable way to call it.",
    change: "Move orchestration into a reusable service and expose it through a small FastAPI web interface.",
    endState: "You can submit a question in the generated `/docs` page and receive a checked report through HTTP.",
    plan: ["Install FastAPI and the local server.", "Move the workflow into one service function.", "Add typed request, preview, and health routes.", "Run one request from the generated API console."],
    unlocks: "Tomorrow you can isolate risky rules and test the API without paying for live model calls.",
  },
  "day-06": {
    startingPoint: "The API works, but important code is tied directly to the live model provider and is difficult to test safely.",
    change: "Put a small interface around model calls and replace the provider with fakes during tests.",
    endState: "Fast unit tests protect citation validation and API behavior without network access or an API key.",
    plan: ["Define the provider interface.", "Pass the provider into planner, researcher, and report writer.", "Test citation rejection and HTTP validation with fakes.", "Run the full unit suite with networking unavailable."],
    unlocks: "Tomorrow you can package a tested Week 1 product instead of a machine-specific script.",
  },
  "day-07": {
    startingPoint: "Week 1 works on your computer, but its Python version and installed packages are still local assumptions.",
    change: "Record dependencies and package the API in a small, non-root Docker image.",
    endState: "Another machine with Docker can build the same image, supply configuration, and run the Week 1 API.",
    plan: ["Record the runtime dependencies.", "Exclude secrets and local files from the image.", "Create the Dockerfile.", "Build, run, and complete the Week 1 quality gate."],
    unlocks: "Week 2 can replace temporary files and memory with a database that survives process restarts.",
  },
  "day-08": {
    startingPoint: "Reports exist only in files or process memory, so they are not a safe coordination point for background work.",
    change: "Start PostgreSQL, connect with async SQLAlchemy, and introduce Alembic for repeatable schema changes.",
    endState: "The app can open a database session, and every future schema change has a versioned migration.",
    plan: ["Start a local PostgreSQL container with a named volume.", "Install the database libraries.", "Add database configuration and an async session factory.", "Initialize Alembic and verify a connection."],
    unlocks: "Tomorrow you can create the tables that represent an entire research run.",
  },
  "day-09": {
    startingPoint: "The database connection works, but PostgreSQL does not yet know what a run, task, source, or report is.",
    change: "Create related tables and database constraints for the full research workflow.",
    endState: "Runs, tasks, sources, evidence, and one final report per run have permanent, queryable records.",
    plan: ["Translate the workflow into database models.", "Add relationships and uniqueness rules.", "Generate and read the migration before applying it.", "Try invalid inserts to prove the constraints."],
    unlocks: "Tomorrow the API can save a queued run and return a tracking ID immediately.",
  },
  "day-10": {
    startingPoint: "The schema can store runs, but the API still performs the slow workflow during the request.",
    change: "Make POST `/runs` save a queued record and return its ID; add separate status and report routes.",
    endState: "A request returns quickly, remains visible after an API restart, and can be checked later with its run ID.",
    plan: ["Create small repository operations for database access.", "Replace the preview route with run resources.", "Return HTTP 202 for accepted work.", "Restart the API and fetch the same queued run."],
    unlocks: "Tomorrow a separate program can pick up and execute those queued runs.",
  },
  "day-11": {
    startingPoint: "The API now creates durable queued rows, but nothing outside the API processes them.",
    change: "Create a polling worker and use a short database lock so two workers cannot claim the same row.",
    endState: "Two worker processes safely divide queued runs, while slow research happens outside the HTTP request.",
    plan: ["Write the atomic claim query.", "Commit the claim before starting slow work.", "Create a stoppable polling loop.", "Run two workers against several queued runs."],
    unlocks: "Tomorrow you can split the large workflow into saved stages that a restarted worker can understand.",
  },
  "day-12": {
    startingPoint: "A worker can claim a run, but one large function cannot show which research stage finished before a crash.",
    change: "Define allowed statuses and save a checkpoint after planning, research, and report writing.",
    endState: "A worker reloads the run status and continues from the correct stage instead of starting blindly from the beginning.",
    plan: ["Define the allowed state transitions.", "Split orchestration into stage functions.", "Save each stage output and its next status together.", "Restart a worker between stages and observe the resume point."],
    unlocks: "Tomorrow you can add retries without repeating finished business work.",
  },
  "day-13": {
    startingPoint: "Stages are saved, but crashes and temporary provider errors can still cause the same stage to run more than once.",
    change: "Track operation identity, retry temporary failures with limits, and recover abandoned in-progress work.",
    endState: "Repeating a completed stage is harmless, permanent failures stop clearly, and temporary failures retry predictably.",
    plan: ["Add a unique operation record for each run stage.", "Classify errors as retryable or permanent.", "Add bounded backoff and attempt counts.", "Run synthesis twice and confirm only one report exists."],
    unlocks: "Tomorrow you can prove the whole durable workflow against a real test database.",
  },
  "day-14": {
    startingPoint: "The durable pieces work individually, but mocks cannot prove that locks, transactions, migrations, and constraints cooperate.",
    change: "Run the real API and worker path against an isolated PostgreSQL test database.",
    endState: "Automated tests prove successful completion, restart safety, citation rejection, and protection from duplicate reports.",
    plan: ["Create a clearly named test database.", "Write one vertical API-to-worker integration test.", "Add one focused test for each failure guarantee.", "Rebuild the test database and run the Week 2 gate."],
    unlocks: "Week 3 can safely add speed and message delivery on top of PostgreSQL's trusted state.",
  },
  "day-15": {
    startingPoint: "PostgreSQL safely stores everything, but repeated searches and frequent progress reads may be slower than necessary.",
    change: "Add Redis as a temporary cache, progress snapshot, and simple learning-level rate limiter.",
    endState: "Common reads become faster, while clearing or stopping Redis never removes an accepted run or final report.",
    plan: ["Start Redis and connect with the async client.", "Create clear, versioned cache keys.", "Treat a cache failure as a cache miss.", "Add and test one small rate-limit counter."],
    unlocks: "Tomorrow you can learn Kafka separately before it becomes responsible for delivering real work.",
  },
  "day-16": {
    startingPoint: "Database polling works, but you need to understand message delivery before replacing it inside EvidenceLab.",
    change: "Build a tiny Kafka producer and consumer and observe partitions, consumer groups, offsets, and redelivery.",
    endState: "You can predict which consumer receives a message and when an unfinished message will be delivered again.",
    plan: ["Start one local Kafka broker and create a topic.", "Publish three keyed learning events.", "Consume them with manual offset commits.", "Run six small restart and multi-consumer observations."],
    unlocks: "Tomorrow you can connect PostgreSQL to Kafka without risking a saved run that never receives a job message.",
  },
  "day-17": {
    startingPoint: "Saving a run and publishing its Kafka message as two separate actions leaves a gap where only one may succeed.",
    change: "Save the run and an outgoing-message row together, then let a relay publish unsent rows to Kafka.",
    endState: "Every accepted run has a durable message intent, and publication can be retried after a crash.",
    plan: ["Add the versioned outbox-event table.", "Create the run and outbox row in one transaction.", "Build a relay for unpublished rows.", "Crash the relay and prove the message is not lost."],
    unlocks: "Tomorrow the first workflow stage—planning—can move into a Kafka worker.",
  },
  "day-18": {
    startingPoint: "Commands now reach Kafka reliably, while planning still happens in the old general worker.",
    change: "Create a planner consumer that validates a message, makes tasks once, and queues the research messages.",
    endState: "Planning runs in its own process, and a repeated Kafka delivery produces no duplicate tasks.",
    plan: ["Validate the event envelope before using it.", "Handle one plan command in a database transaction.", "Write one search command per task to the outbox.", "Commit Kafka progress only after PostgreSQL succeeds."],
    unlocks: "Tomorrow several research workers can share the independent search commands.",
  },
  "day-19": {
    startingPoint: "The planner produces independent research commands, but one consumer would still process them in sequence.",
    change: "Use Kafka partitions and one consumer group so three research workers can share the tasks.",
    endState: "Research happens in parallel, every evidence result is saved once, and Redis progress remains optional.",
    plan: ["Create enough topic partitions for parallel workers.", "Make the research handler safe to repeat.", "Start three named workers in one group.", "Observe task distribution, saved evidence, and progress."],
    unlocks: "Tomorrow the last completed research task can safely request one final report.",
  },
  "day-20": {
    startingPoint: "Several workers finish research independently, so more than one may notice that the run is ready for a report.",
    change: "Use a unique synthesis operation so that race creates only one report-writing job and one saved report.",
    endState: "Duplicate completion messages are harmless, and the final report still passes the Day 4 citation check.",
    plan: ["Create synthesis intent under a unique database key.", "Publish the report-writing command through the outbox.", "Load evidence and write the report in a synthesis worker.", "Deliver completion twice and confirm one durable report."],
    unlocks: "Tomorrow prompts and model choices can move out of copied code into controlled, versioned settings.",
  },
  "day-21": {
    startingPoint: "Workers are separate, but prompt text and model settings are copied into deployments and hard to change safely.",
    change: "Add a small Django Admin area for versioned prompts and model profiles.",
    endState: "An internal user can activate configuration without rebuilding workers, and every run records which version produced it.",
    plan: ["Create only the Django project and prompt app you need.", "Model prompt versions and model profiles.", "Make activation of one version atomic.", "Let workers read configuration and save its identity with output."],
    unlocks: "Week 4 can package all databases, brokers, APIs, workers, and admin processes into one reproducible local setup.",
  },
  "day-22": {
    startingPoint: "The distributed workflow works, but starting it requires several terminals and remembering service addresses by hand.",
    change: "Describe every local service in Docker Compose and run all Python roles from one shared image.",
    endState: "One documented command starts the full system and completes a run without manually launching Python processes.",
    plan: ["Change container URLs to Compose service names.", "Describe PostgreSQL, Redis, Kafka, API, workers, relay, and admin.", "Run database migrations once as a separate job.", "Start from a clean state and complete one research run."],
    unlocks: "Tomorrow you can add logs and measurements that explain what all those processes are doing.",
  },
  "day-23": {
    startingPoint: "The whole system starts together, but scattered print statements cannot explain one run across many processes.",
    change: "Add structured logs, shared run IDs, a few useful counters, and stage timers.",
    endState: "You can follow one failed run from API acceptance to its final state and measure behavior across all runs.",
    plan: ["Emit consistent JSON log fields.", "Carry run and event IDs through every process.", "Expose a small set of bounded metrics.", "Break one call and reconstruct its path from the logs."],
    unlocks: "Tomorrow you can learn Kubernetes with enough visibility to understand its behavior instead of guessing.",
  },
  "day-24": {
    startingPoint: "Compose runs fixed processes on one computer; Kubernetes concepts are still unfamiliar and should not be mixed with app debugging yet.",
    change: "Use a disposable web server to learn namespaces, Pods, Deployments, Services, scaling, and automatic replacement.",
    endState: "You can inspect, scale, break, and repair a tiny Kubernetes deployment and explain what each object does.",
    plan: ["Confirm which local cluster your commands target.", "Create an isolated EvidenceLab namespace.", "Deploy and expose a disposable nginx server.", "Delete a Pod and try one invalid image tag while observing events."],
    unlocks: "Tomorrow you can deploy the real EvidenceLab API and worker using concepts you have already observed.",
  },
  "day-25": {
    startingPoint: "You understand the Kubernetes objects, but EvidenceLab still runs only through Compose.",
    change: "Deploy the API and a research worker from the same image with different commands and separate scaling.",
    endState: "Two API copies sit behind one stable Service, while the worker runs privately and can scale independently.",
    plan: ["Create non-secret configuration and a safely supplied API secret.", "Deploy two API replicas behind a Service.", "Deploy the research worker without an inbound Service.", "Apply the files and scale API and worker separately."],
    unlocks: "Tomorrow you can make health checks and shutdown behavior truthful during restarts and updates.",
  },
  "day-26": {
    startingPoint: "Kubernetes can run the processes, but a shallow health check or abrupt worker stop can still route bad traffic or lose unfinished work.",
    change: "Separate liveness from readiness, handle worker shutdown, and watch a rolling update.",
    endState: "Traffic reaches only ready API Pods, stuck processes restart, and workers do not acknowledge work they failed to save.",
    plan: ["Keep liveness local and cheap.", "Make readiness check only critical dependencies with a short timeout.", "Stop Kafka consumers gracefully on termination.", "Perform a harmless rolling update while watching availability."],
    unlocks: "Tomorrow you can test the recovery promises by deliberately stopping real components.",
  },
  "day-27": {
    startingPoint: "The design includes retries and recovery rules, but they are still claims until you observe failures.",
    change: "Predict, inject, observe, and record five safe failures against clear data-safety rules.",
    endState: "A reproducible failure report shows what survives, how recovery happens, and which fixes were necessary.",
    plan: ["Create the experiment record before testing.", "Delete an API Pod and crash a research worker.", "Repeat a message and stop Redis.", "Return malformed model output and compare every result with your prediction."],
    unlocks: "Tomorrow you can measure report quality as carefully as you measured infrastructure reliability.",
  },
  "day-28": {
    startingPoint: "The infrastructure recovers well, but a reliable pipeline can still produce a weak or misleading report.",
    change: "Run 15 varied questions through code checks and a human scoring guide.",
    endState: "You have a repeatable quality baseline, category-level scores, and a list of the most common report failures.",
    plan: ["Write a balanced 15-question evaluation set.", "Automate citation and output-shape rules.", "Score support, coverage, source quality, balance, and clarity by hand.", "Save per-category results, cost, and failure patterns."],
    unlocks: "Tomorrow your documentation can use real test, failure, and evaluation evidence instead of broad claims.",
  },
  "day-29": {
    startingPoint: "The system and its evidence exist, but a new reader would have to inspect the code to understand the engineering story.",
    change: "Write a reader-first README with one clear diagram, reproducible setup, decisions, tradeoffs, and proof.",
    endState: "Another engineer can understand, start, inspect, and discuss EvidenceLab without private guidance from you.",
    plan: ["Put the useful product and short demo first.", "Draw one honest end-to-end architecture diagram.", "Explain six decisions with alternatives and consequences.", "Follow the setup literally from a clean folder and fix every missing step."],
    unlocks: "Tomorrow you can freeze the exact build, rehearse its story, and tag the portfolio release.",
  },
  "day-30": {
    startingPoint: "You have a release candidate, documentation, failure evidence, and an evaluation baseline.",
    change: "Stop adding features, run the release checks, rehearse the demonstration, state limitations, and tag the tested commit.",
    endState: "EvidenceLab v1 is reproducible, honestly scoped, and ready for a calm 8–10 minute portfolio demonstration.",
    plan: ["Run tests, rebuild cleanly, and scan for secrets.", "Practice the timed happy-path and failure story.", "Write specific limitations and credible next steps.", "Tag only the exact commit that passed every release check."],
    unlocks: "The 30-day build is complete. Future work should respond to evaluation results or real user needs, not add tools for appearance.",
  },
};
